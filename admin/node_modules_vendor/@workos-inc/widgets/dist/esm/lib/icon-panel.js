import { jsx } from "react/jsx-runtime";
import { Flex } from "@radix-ui/themes";
import { forwardRef } from "react";
const IconPanel = forwardRef(function IconPanel2({ children, style, ...props }, ref) {
  return /* @__PURE__ */ jsx(
    Flex,
    {
      ref,
      width: "32px",
      height: "32px",
      align: "center",
      justify: "center",
      style: {
        borderWidth: 1,
        borderStyle: "solid",
        borderColor: "var(--gray-4)",
        borderRadius: "var(--radius-3)",
        backgroundColor: props.color === "panel" ? "var(--color-panel)" : "var(--gray-2)",
        ...style
      },
      ...props,
      children
    }
  );
});
export {
  IconPanel
};
//# sourceMappingURL=icon-panel.js.map