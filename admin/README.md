To get started with the admin follow this steps:

1. From the `admin` directory run `npm install` to install React, Lit and the other node modules.
1. From the `admin` directory run `npm start` to run the app in development mode at `http://localhost:3000`.
