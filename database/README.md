# database

> NOTE (Gianluca): This directory may no longer be needed after the Meergo
> initialization scripts have been embedded into the executable.
> 
> So, consider what to do with it once we've completed the modification that
> embeds the SQL scripts.

## About the [`updates.sql`](updates.sql) file

The [`updates.sql`](updates.sql) file contains various update queries for the PostgreSQL database and is used internally by Meergo developers.
