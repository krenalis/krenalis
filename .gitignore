# Ignore the binaries produced by `go build`.
/krenalis
/krenalis.exe

# Since '.env' files are used for Krenalis configuration and may contain
# critical information, it's convenient to add this repository-wide exclusion
# rule here, just as an extra precaution to prevent '.env' files containing
# sensitive data from ending up on GitHub.
.env