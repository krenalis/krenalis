on:
  workflow_dispatch:

  # push:
  #   branches:
  #     - main

jobs:
  tests:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

    # TODO: in the branch with Sentry integration in the admin, develop this
    # part here.
